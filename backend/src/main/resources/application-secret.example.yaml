# Copy to application-secret.yaml and fill in real values (never commit application-secret.yaml).

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/portfolio
    username: root
    password: your-password
    driver-class-name: org.mariadb.jdbc.Driver

jwt:
  secret: change-me-to-at-least-32-characters-long-random-string
  expiration-ms: 3600000
